apiVersion: apps/v1
kind: Deployment
metadata:
  name: ivolve-project-deployment-last
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ivolve-project-last
  template:
    metadata:
      labels:
        app: ivolve-project-last
    spec:
      containers:
      - name: jenkins-project
        image: 3omda2/final-project:latest
        ports:
        - containerPort: 8081
